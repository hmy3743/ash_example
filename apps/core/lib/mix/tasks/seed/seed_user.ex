defmodule Mix.Tasks.SeedUser do
  # alias Core.Accounts.User

  def seed do
    IO.puts("Seeding users...")
    # User.sign_up!("link@cona.com", "cona2024!")
    # User.sign_up!("zelda@cona.com", "cona2024!")
  end
end
